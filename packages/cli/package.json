{
  "name": "@hackl/cli",
  "version": "0.3.3",
  "description": "Hackl CLI: local-first AI coding agent in the terminal.",
  "license": "MIT",
  "private": true,
  "bin": {
    "hackl": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "compile": "tsc -b",
    "build": "tsc -b && node esbuild.mjs",
    "bundle": "node esbuild.mjs",
    "test": "tsc -b && node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "@hackl/core": "*",
    "@hackl/server": "*",
    "@modelcontextprotocol/sdk": "^1.21.0"
  }
}
